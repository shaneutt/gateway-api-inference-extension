apiVersion: v1
kind: Service
metadata:
  name: endpoint-picker
spec:
  type: ClusterIP
  selector:
    app: endpoint-picker
  ports:
    - name: grpc
      protocol: TCP
      port: 9080
      targetPort: 9080
